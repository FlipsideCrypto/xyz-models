version: 2
models:
  - name: price__ez_hourly_token_prices
    description: A comprehensive fact table holding provider and hourly token prices for Bitcoin, with the difference from the fact table being only one provider is reported per hour.

    columns:
      - name: HOUR
        description: "{{ doc('hour') }}"
        tests:
          - not_null
          - unique

      - name: PRICE
        description: "{{ doc('price') }}"
        tests:
          - not_null

      - name: PROVIDER
        description: "{{ doc('provider') }}"
