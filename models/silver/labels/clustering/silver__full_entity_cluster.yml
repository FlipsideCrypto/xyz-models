version: 2
models:
  - name: silver__full_entity_cluster
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address

    columns:
      - name: ADDRESS
              
      - name: address_group

      - name: project_name

      - name: _inserted_timestamp
