version: 2

sources:
  - name: crosschain
    database: "{{ 'crosschain' if target.database == 'evmos' else 'crosschain_dev' }}"
    schema: core
    tables:
      - name: dim_date_hours
      - name: address_tags
      - name: dim_dates
  - name: bronze 
    database: evmos
    schema: bronze 
    tables: 
      - name: sample_blocks
      - name: sample_txs
